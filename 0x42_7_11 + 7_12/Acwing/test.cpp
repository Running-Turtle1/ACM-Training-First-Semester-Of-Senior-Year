#include<bits/stdc++.h>
using namespace std;
#define int long long

void solve(){
	cout << (1.0 - 2.0) / 12 << '\n';
	double x = 1.0;
	cout << (++ x);
}

signed main(){
	ios::sync_with_stdio(false);
	cin.tie(0), cout.tie(0); 
	solve();
	return 0;
}
